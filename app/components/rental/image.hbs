<button type="button" class="image {{if this.isLarge " large"}}" {{on "click" this.toggleSize}}>
    <img ...attributes>
    {{!-- {{#if this.isLarge}}
    <small>View Smaller</small>
    {{else}}
    <small>View Larger</small>
</button>
{{/if}} --}}
 <small>View {{if this.isLarge "Smaller" "Larger"}}</small>
</button>
{{!-- Em vez de codificar valores específicos para os atributos srce altna <img>tag, optamos pela ...attributes
palavra-chave, que às vezes também é chamada de sintaxe "splattributes" .
Isso permite que atributos HTML arbitrários sejam passados ​​ao invocar este componente, --}}